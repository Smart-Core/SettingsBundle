
<div class="box box-success">
    <div class="box-header with-border">
        {#<div >#}
            <a href="{{ path('smart_core_setting_history', {'id': setting.id}) }}" class="btn btn-default" style="float: right;">История измнений</a>
        {#</div>#}

        <h3>Редактирование настройки: &laquo;{{ setting }}&raquo;</h3>

        {{ form_errors(form) }}

        {{ form_start(form, {'attr': {'class': 'well'} }) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}

    </div>
</div>
